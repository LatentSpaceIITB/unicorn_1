/**
 * useAudio Hook
 *
 * Re-export from AudioContext for easier imports.
 * Usage: import { useAudio } from '@/hooks/useAudio';
 */

export { useAudio } from '@/contexts/AudioContext';
export type { MusicState } from '@/lib/audioManager';
